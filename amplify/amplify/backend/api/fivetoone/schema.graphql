type RecordTable @model @auth(rules: [{ allow: owner }]){
  id: ID!
  name: String!
  createdAt: AWSDateTime
  description: String
  category: String
  department: String
  shift: String
  agreement: String
  sentiment: String
  comment: String
}
